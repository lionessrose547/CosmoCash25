import { Roommate, Expense, WishlistItem, ChatMessage } from './types';

// Mock data has been removed to make the app dynamic.
// App state is now initialized as empty and persisted in localStorage.

export const MOCK_ROOMMATES: Roommate[] = [];
export const MOCK_EXPENSES: Expense[] = [];
export const MOCK_WISHLIST_ITEMS: WishlistItem[] = [];
export const MOCK_CHAT_MESSAGES: ChatMessage[] = [];
